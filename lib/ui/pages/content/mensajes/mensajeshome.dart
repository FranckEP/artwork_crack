import 'package:flutter/material.dart';

class homeMensaje extends StatelessWidget {
  const homeMensaje({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Aquí van los mensajes'),
    );
  }
}